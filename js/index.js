// document.querySelectorAll('label.text').forEach((element) =>  {
//   //  console.log(element);
// element.addEventListener('click', (e) => {
// console.log(e.target);
//     document.querySelectorAll('.showBorder').forEach((element) => {
//         element.classList.remove('showBorder');
//     });
//     e.target.querySelector('.noBorder').classList.add('showBorder');

// })}
// );